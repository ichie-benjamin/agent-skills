# Direction

Copy this template, fill it, run Validator A (SKILL.md §Validators), submit when it passes. Skip sections marked `–` in SKILL.md §Section requirements matrix for the type. No "TBD" in required sections — ask the human instead.

## Header
- Slug: `<kebab-case>`
- Type: `<one of 6>`
- Duration: `<exact seconds>`
- Desktop · Mobile aspect: `<16:9 | 1:1>` · `<9:16 | N/A>`
- Channels: `<named>`
- Audience: `<merchant/segment | customer/segment>`
- CTA (verbatim): `<exact>`
- Approval gate: `<named human>`

## Concept
One sentence. One idea.

## Signature device
The repeating object or transformation that runs through every scene.

## Visual signature moves
Count per matrix. Each cites a reference.

| Move | Where it lands | Reference lifted from |
|---|---|---|

## Audio signature
Count per matrix + music character.

| Element | Where | Source |
|---|---|---|

Music character: `<one line>`

## Look
- Palette: `bg <hex> · ink <hex> · accent #00A884 · supporting <hex>`
- Type: `hero <font + size band> · body <font + size> · UI <font + size>` · files `<built-in | fonts/*.woff2>`
- Corners · depth: `<sharp / 8px / 16px / pill>` · `<flat / subtle / layered>`
- Motion feel: `<eases + duration band>`

## Story skeleton
One of Three-Act · STAR · PIUR · Before-After · Promised Land. One-line reason for the pick.

## Energy arc
Peak `<scene>` · Quiet beat `<scene>` · Resolve `<method>`. Apply per matrix.

## References
At least one local file path.

| Reference | Path / URL | What we lift |
|---|---|---|

## Production resources
- Screen-cap available: `<yes / which screens | no>`
- Face-cam: `<yes / no>`
- Banned this project: `<list or empty>`

## Banned / guard
Brand bans (filler · hedges · competitors · fabricated numbers · synthetic Queek UI) and any project-specific bans.

---

`Direction validator: <passed>/<applicable> ✓ [fails: <list> | none]`
